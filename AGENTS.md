# Scan validation

Use real radiographs and actual model inference when validating segmentation or
checking the app. Do not generate synthetic scans, or substitute fabricated
landmarks or masks as evidence that segmentation works. Visually inspect the
resulting overlays on the real images and report unresolved failures explicitly.
